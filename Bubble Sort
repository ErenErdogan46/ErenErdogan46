#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin>>n;
    int arr[n];
    for(int i = 0; i<n; i++)
    {
        cin>>arr[i];
    }
    for(int j = 0; j<n; j++)
    {
        for(int m=0; m<n-1; m++)
        {
            if(arr[m]>arr[m+1]){swap(arr[m],arr[m+1]);}
        }
    }
    for(auto o:arr)
    {
        cout<<o<<" ";
    }
    return 0;
}
